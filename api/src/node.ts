/** Node CLI / deploy entry — uses absolute zk asset paths. */
export { AetheriaAPI } from './index.js';
export {
  CompiledAetheriaContract,
  ledger,
  zkConfigPath,
} from '../../contract/index.js';
